(()=>{
	return	{
		makeBusy: true,
		cmd: "autofish",
		alias: ['af'],
		usage: "af",
		description: "Automatically Fishes",
		handler: async function(sender, args){
			sender.busy = true;
			// Find Waterblock Position
				// Look at Water
					// Fish
						// on Collect
							// Clear Inventory into water
					// out of rods -> quit
		}
	}
})()
